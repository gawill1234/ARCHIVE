Gronk is now in newgronk.  To build for each platform:

make linux
make solaris
make windows

Choose what is appropriate from above.  The old version of gronk does
not support a number of functions which the tests will look for
(like checking for core files, etc).

